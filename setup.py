from setuptools import setup, find_packages

setup(
    name='energy-level-diagram',
    version='0.1.0',
    description='Plotter for energy levels with column and connection control',
    packages=find_packages(),
    install_requires=['matplotlib'],
)
